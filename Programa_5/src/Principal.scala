import scala.io.StdIn._

/*
  5.- Programa que muestre cuantos son los divisores enteros entre dos números dados.
 */

object Principal {
  def main(args: Array[String]): Unit = {
    print("\n\tDIVISORES ENTRE DOS NÚMEROS\nNumero 1: ")
    var numeroUno = readInt()
    print("Numero 2: ")
    var numeroDos = readInt()
    divisores(numeroUno,numeroDos)
  }

  def divisores(uno: Int, dos: Int): Unit = {
    /*
      No entendi lo que solicita el programa.
     */
  }

}
